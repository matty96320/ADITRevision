{
  "name": "tpgrevision",
  "version": "1.0.0",
  "description": "OECD TP Master 2022 - Question Generation App",
  "type": "module",
  "scripts": {
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "build": "cd client && npm run build && cd .. && cd server && npm run build",
    "start": "cd server && npm start"
  },
  "dependencies": {
    "@google/genai": "^1.30.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}